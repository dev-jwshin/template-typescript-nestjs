import { IsString, IsOptional, IsBoolean, IsInt, IsEmail, IsUUID } from 'class-validator';

/**
 * User 생성 DTO
 *
 * class-validator 데코레이터를 사용하여 입력 값을 검증합니다.
 */
export class CreateUserDto {
  /**
   * TODO: 필요한 필드 정의
   *
   * @example
   * @IsString()
   * name: string;
   *
   * @IsEmail()
   * email: string;
   *
   * @IsOptional()
   * @IsBoolean()
   * isActive?: boolean;
   */
}
